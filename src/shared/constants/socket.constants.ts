export enum SocketEventsEnum {
  NOTIFICATION = 'notification',
  INSTANCE = 'instance',
  USER = 'user',
}

export enum SocketNotificationStatusEnum {
  INFO = 'info',
  SUCCESS = 'success',
  WARNING = 'warning',
  ERROR = 'error',
}

export enum SocketNotificationTypeEnum {
  SYSTEM = 'system',
  USER = 'user',
}

export enum SocketUserEventTypesEnum {
  DATA = 'data',
  UPDATE = 'update',
  LOGOUT = 'logout',
  ERROR = 'error',
  AUTHENTICATION = 'authentication',
}
